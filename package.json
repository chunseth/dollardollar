{
  "name": "first-dollar",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test && node --check app.js && node --check beliefs.js && node --check env.js && node --check server.js && node --check onboarding.js && node --check scripts/seed.js",
    "db:migrate": "node scripts/migrate.js",
    "db:seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@openai/agents": "^0.14.3",
    "@openai/agents-extensions": "^0.14.3",
    "@openai/codex-sdk": "^0.147.0",
    "pg": "^8.16.3",
    "zod": "^4.4.3"
  }
}
